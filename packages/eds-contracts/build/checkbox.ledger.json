[
  {
    "fact": "glyph-off is a glyph: footprint = cap box, ink overflows",
    "css": "CARRIED",
    "figma": "LOWERED",
    "note": "CSS: margin = (footprint − glyph)/2, negative, travels with the element; Figma: footprint-sized auto-layout container, glyph in flow, centered, clip off."
  },
  {
    "fact": "glyph-on is a glyph: footprint = cap box, ink overflows",
    "css": "CARRIED",
    "figma": "LOWERED",
    "note": "CSS: margin = (footprint − glyph)/2, negative, travels with the element; Figma: footprint-sized auto-layout container, glyph in flow, centered, clip off."
  },
  {
    "fact": "glyph-mixed is a glyph: footprint = cap box, ink overflows",
    "css": "CARRIED",
    "figma": "LOWERED",
    "note": "CSS: margin = (footprint − glyph)/2, negative, travels with the element; Figma: footprint-sized auto-layout container, glyph in flow, centered, clip off."
  },
  {
    "fact": "prop checked (unchecked|checked|indeterminate)",
    "css": "CARRIED",
    "figma": "LOWERED",
    "note": "CSS: [data-<axis>='<value>'] attributes, default = attribute absent (ADR-0006); Figma: Checked variant axis."
  },
  {
    "fact": "state focus",
    "css": "CARRIED",
    "figma": "REFUSED",
    "note": "CSS: :focus-visible — the ring is the PLATFORM's, not a designable state. A canvas has no keyboard; nobody instantiates State=Focus. Refused, not lowered (Marco Krenn's point, 2026-08-30)."
  },
  {
    "fact": "state disabled",
    "css": "CARRIED",
    "figma": "LOWERED",
    "note": "CSS: :disabled; Figma: State variant (no interactivity on canvas)."
  },
  {
    "fact": "semantics: <input>",
    "css": "CARRIED",
    "figma": "REFUSED",
    "note": "A canvas has no accessibility tree. Refused, not dropped — the fact stays in the contract."
  }
]
